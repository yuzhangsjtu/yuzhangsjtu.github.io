---
// Minimal footer: only the theme/locale switchers remain — they have no
// other entry point in the UI.
import type { Locale } from '~/i18n/config'
import LocaleSwitcher from './LocaleSwitcher.astro'
import ThemeSwitcher from './ThemeSwitcher.astro'

interface Props {
  locale: Locale
  pathname: string
}

const { locale, pathname } = Astro.props
---

<!-- Background inherits the page root bg — no tinted band. -->
<footer data-hide-print class="relative z-[1] mt-6 pb-6 pt-4 md:pt-6">
  <div class="px-4 sm:px-8">
    <div class="mx-auto min-w-0 max-w-7xl lg:px-8">
      <div class="flex items-center gap-3 text-copy-13 text-neutral-6">
        <ThemeSwitcher locale={locale} />
        <span aria-hidden class="select-none text-neutral-4">|</span>
        <LocaleSwitcher locale={locale} pathname={pathname} />
      </div>
    </div>
  </div>
</footer>
